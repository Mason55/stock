# constraints.txt - 版本约束确保跨平台一致性
# 使用方法: pip install -r requirements.txt -c constraints.txt

# Python版本兼容性
# 需要 Python >= 3.8, < 3.12

# 核心框架约束
Flask==2.3.2
Werkzeug==2.3.6
Jinja2==3.1.2
MarkupSafe==2.1.3

# 数据库驱动约束
SQLAlchemy==2.0.19
psycopg2-binary==2.9.7

# 数据处理核心版本
pandas==2.0.3
numpy==1.24.3

# 机器学习版本锁定 (避免兼容性问题)
scikit-learn==1.3.0
lightgbm==4.0.0
xgboost==1.7.6

# 配置和序列化
pydantic==2.1.1
pydantic-settings==2.0.2
python-dotenv==1.0.0

# 测试框架版本
pytest==7.4.0
pytest-cov==4.1.0

# 网络库
requests==2.31.0
aiohttp==3.8.5

# 缓存和队列
redis==4.6.0
celery==5.3.1

# 开发工具版本锁定
black==23.7.0
isort==5.12.0
flake8==6.0.0